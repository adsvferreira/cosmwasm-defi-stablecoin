#!/usr/bin/env node
import "source-map-support/register";
import { RuntimeArgs, TaskArguments, WasmkitRuntimeEnvironment } from "../../types";
import { PackageJson } from "../util/packageInfo";
import { ArgumentsParser } from "./arguments-parser";
interface EnvAndArgs {
    env: WasmkitRuntimeEnvironment;
    taskName: string;
    taskArguments: TaskArguments;
}
interface RuntimeArgsAndPackageJson {
    runtimeArgs: RuntimeArgs;
    unparsedCLAs: string[];
    maybeTaskName: string | undefined;
    showStackTraces: boolean;
    packageJson: PackageJson;
    argumentsParser: ArgumentsParser;
}
export declare function gatherArguments(): Promise<RuntimeArgsAndPackageJson>;
export declare function loadEnvironmentAndArgs(maybeTaskName: string | undefined, runtimeArgs: RuntimeArgs, argumentsParser: ArgumentsParser, unparsedCLAs: string[]): Promise<EnvAndArgs>;
export {};
